<section id="tarifs" class="py-20 lg:py-28">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <span class="text-sm font-semibold text-primary-light uppercase tracking-wider">Tarifs</span>
      <h2 class="mt-3 text-3xl sm:text-4xl font-bold text-white">
        Commence <span class="text-cta">gratuitement</span>, évolue quand tu veux
      </h2>
      <p class="mt-4 text-gray-400 text-lg max-w-2xl mx-auto">
        Pas de piège, pas de carte bancaire. Tu utilises TitanBTP gratuitement aussi longtemps que tu veux.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
      <!-- Plan Gratuit -->
      <div class="relative bg-surface-light/50 border-2 border-primary/40 rounded-2xl p-8">
        <div class="absolute -top-3 left-6">
          <span class="bg-primary text-white text-xs font-bold uppercase tracking-wider px-3 py-1 rounded-full">
            Populaire
          </span>
        </div>
        <div class="mb-6">
          <h3 class="text-2xl font-bold text-white">Gratuit</h3>
          <div class="mt-4 flex items-baseline gap-1">
            <span class="text-5xl font-extrabold text-white">0 €</span>
            <span class="text-gray-400">/mois</span>
          </div>
          <p class="mt-2 text-sm text-gray-400">Pour toujours. Aucune limite de temps.</p>
        </div>

        <ul class="space-y-3 mb-8">
          {
            [
              'Devis & factures illimités',
              'Estimation IA (5 / mois)',
              'Planning vue liste & mois',
              'Gestion de projets',
              'CRM Pipeline',
              '1 utilisateur',
              'Catalogue de tarifs',
              'Support par email',
            ].map((feat) => (
              <li class="flex items-start gap-3 text-sm text-gray-300">
                <svg class="w-5 h-5 text-cta shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" /></svg>
                {feat}
              </li>
            ))
          }
        </ul>

        <a
          href="https://app.titanbtp.com/auth"
          class="block w-full text-center bg-cta hover:bg-cta-dark text-gray-900 font-semibold rounded-xl px-6 py-3.5 transition-all hover:shadow-lg hover:shadow-cta/25"
        >
          Commencer gratuitement
        </a>
      </div>

      <!-- Plan Pro -->
      <div class="bg-surface-light/30 border border-white/10 rounded-2xl p-8 relative overflow-hidden">
        <div class="absolute top-0 right-0 w-32 h-32 bg-primary/5 rounded-full -translate-y-1/2 translate-x-1/2"></div>
        <div class="mb-6">
          <h3 class="text-2xl font-bold text-white">Pro</h3>
          <div class="mt-4 flex items-baseline gap-1">
            <span class="text-4xl font-extrabold text-gray-400">Bientôt</span>
          </div>
          <p class="mt-2 text-sm text-gray-500">Prix et détails annoncés prochainement.</p>
        </div>

        <ul class="space-y-3 mb-8">
          {
            [
              'Tout le plan Gratuit, plus :',
              'Équipe illimitée',
              'Estimation IA illimitée',
              'Planning vue semaine',
              'Gestion multi-entreprise',
              'Catalogue Artiprix intégré',
              'Support prioritaire',
              'API & intégrations',
            ].map((feat, i) => (
              <li class:list={['flex items-start gap-3 text-sm', i === 0 ? 'text-primary-light font-medium' : 'text-gray-500']}>
                {i > 0 && (
                  <svg class="w-5 h-5 text-gray-600 shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" /></svg>
                )}
                {feat}
              </li>
            ))
          }
        </ul>

        <button
          disabled
          class="block w-full text-center bg-white/5 text-gray-500 font-semibold rounded-xl px-6 py-3.5 cursor-not-allowed border border-white/10"
        >
          Bientôt disponible
        </button>
      </div>
    </div>
  </div>
</section>
