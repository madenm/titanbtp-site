---
import BaseLayout from '../layouts/BaseLayout.astro';

const title = 'Contact — TitanBTP | Parle-nous de ton projet';
const description = 'Une question sur TitanBTP ? Contacte-nous par email ou via le formulaire. Notre équipe te répond rapidement.';
---

<BaseLayout title={title} description={description}>
  <section class="pt-28 pb-20 lg:pt-40 lg:pb-28">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <span class="text-sm font-semibold text-primary-light uppercase tracking-wider">Contact</span>
        <h1 class="mt-4 text-4xl sm:text-5xl font-extrabold text-white">
          Une question ?<br />
          <span class="text-primary-light">On est là.</span>
        </h1>
        <p class="mt-4 text-gray-400 text-lg">
          Envoie-nous un message et on te répond dans les plus brefs délais.
        </p>
      </div>

      <!-- Contact info -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-6 mb-12">
        <a
          href="mailto:contact@titanbtp.com"
          class="flex items-center gap-3 bg-surface-light/50 border border-white/5 rounded-xl px-6 py-4 hover:border-primary/30 transition-colors"
        >
          <svg class="w-5 h-5 text-primary-light" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" /></svg>
          <span class="text-sm text-gray-300">contact@titanbtp.com</span>
        </a>
      </div>

      <!-- Form -->
      <form
        class="bg-surface-light/50 border border-white/5 rounded-2xl p-6 sm:p-8 space-y-6"
        action="#"
        method="POST"
      >
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
          <div>
            <label for="name" class="block text-sm font-medium text-gray-300 mb-2">Nom</label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full bg-surface border border-white/10 rounded-xl px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-primary/50 focus:ring-1 focus:ring-primary/50 transition-colors"
              placeholder="Ton nom"
            />
          </div>
          <div>
            <label for="email" class="block text-sm font-medium text-gray-300 mb-2">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full bg-surface border border-white/10 rounded-xl px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-primary/50 focus:ring-1 focus:ring-primary/50 transition-colors"
              placeholder="ton@email.com"
            />
          </div>
        </div>
        <div>
          <label for="message" class="block text-sm font-medium text-gray-300 mb-2">Message</label>
          <textarea
            id="message"
            name="message"
            rows="5"
            required
            class="w-full bg-surface border border-white/10 rounded-xl px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-primary/50 focus:ring-1 focus:ring-primary/50 transition-colors resize-none"
            placeholder="Décris-nous ton besoin ou ta question..."
          ></textarea>
        </div>
        <button
          type="submit"
          class="w-full sm:w-auto bg-cta hover:bg-cta-dark text-gray-900 font-semibold rounded-xl px-8 py-3.5 transition-all hover:shadow-lg hover:shadow-cta/25"
        >
          Envoyer le message
        </button>
      </form>

      <!-- Calendly placeholder -->
      <div class="mt-12 text-center">
        <p class="text-gray-500 text-sm">
          Tu préfères discuter de vive voix ?
          <a href="#" class="text-primary-light hover:text-white transition-colors underline">
            Réserve un créneau
          </a>
          (Calendly — bientôt disponible)
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
